# ROUTE-IQ Backend Service

FastAPI-powered tactical microservices backend handling corridor telemetry, risk calculation pipelines, and role-based authentication.

## Getting Started
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```
